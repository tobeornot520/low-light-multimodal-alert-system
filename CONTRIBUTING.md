# 协作说明

## 开始工作

1. 从最新默认分支创建短期功能分支，例如 `feature/nir-adapter`、`fix/replay-report` 或 `docs/experiment-protocol`。
2. 一个分支只处理一个明确任务；提交中不要混入虚拟环境、模型、实验数据或无关格式化。
3. 改动公共数据结构或配置时，同步更新上下游代码、示例配置、文档和测试。
4. 向默认分支合并前由至少一名组员复核。

```bash
git switch main
git pull --ff-only
git switch -c feature/short-description
```

## 本地检查

提交前至少运行：

```bash
python -m pytest
python -m ruff check .
git diff --check
```

涉及摄像头、模型、NIR 或边缘设备的改动，还应记录设备型号、配置、输入 manifest、软件版本和可复现实验结果。自动化测试通过不等于实机验收通过。

## 提交与合并

提交信息建议使用以下前缀：

```text
feat: 新功能
fix: 缺陷修复
test: 测试补充
docs: 文档更新
chore: 工程维护
```

推送分支并创建 Pull Request：

```bash
git add <明确的文件路径>
git commit -m "feat: describe the change"
git push -u origin HEAD
```

Pull Request 需说明目标、主要改动、验证结果、影响的接口，以及尚未完成的实机或数据验证。合并公共接口前，相关模块成员应确认字段、单位、时间语义和失败状态。

## 禁止提交的内容

- 真人视频、照片、人脸特征模板、行为标注、事件日志和同意书；
- 姓名、学号、手机号、身份证号等直接身份标识；
- `.env`、访问令牌、密码、私钥和设备凭据；
- ONNX 权重、数据库、虚拟环境、缓存和构建产物；
- 未确认许可允许再发布的公开数据集内容。

发现误提交敏感数据时，不要只用普通删除提交处理。立即停止推送和传播，通知仓库管理员撤销凭据或隔离数据，再根据影响范围处理历史记录。

## 接口约定

- 配置和 manifest 必须通过结构化解析与校验，不能依赖自由文本约定；
- 时间字段写明时钟来源和单位，跨设备数据必须记录同步偏差；
- 身份输出保留 `registered`、`unknown`、`uncertain` 和不可用状态；
- 多模态冲突时保守降级，不以单路高分强行覆盖冲突；
- 事件输出保留原因、证据、阈值或规则版本，便于复核；
- 行为识别在同一轨迹上关联，不能仅凭身份提高预警等级。
